CREATE DATABASE IF NOT EXISTS pasteleria_db
    CHARACTER SET utf8mb4
    COLLATE utf8mb4_unicode_ci;

-- Ejecuta lo siguiente con un usuario administrador de MySQL si todavía
-- no creaste el usuario de la aplicación. Cambia la contraseña antes de usarlo.
-- CREATE USER IF NOT EXISTS 'pasteleria_user'@'localhost' IDENTIFIED BY 'TU_PASSWORD';
-- GRANT ALL PRIVILEGES ON pasteleria_db.* TO 'pasteleria_user'@'localhost';
-- FLUSH PRIVILEGES;
